收到用户消息
│
▼
组装: systemPrompt + 摘要 + 未压缩消息 + 当前消息
│
▼
┌─────────────────────────────┐
│ 🔒 Token校验 │
│ countTokens > TOKEN_LIMIT? │
│ YES → 丢弃最早非system消息 │
│ 增量减去token │
│ 循环直到不超限 │
│ NO → 通过 │
└─────────────┬───────────────┘
│
▼
┌─────────────────────────────┐
│ 🔔 条数校验 │
│ 原始未压缩数 ≥ THRESHOLD? │
│ YES → 异步触发压缩(不阻塞)│
│ NO → 跳过 │
└─────────────┬───────────────┘
│
▼
发送给大模型

文件提取文本

.搜索对话框模糊匹配

.用户头像，名称
删除会话
